# APTERRA Compendium Requirements & Evidence Ledger

This is the single authoritative Phase 1 traceability/evidence ledger. The latest rebuilt compendium is the [20-page APTERRA_MASTER_COMPENDIUM_REBUILT.pdf](../../APTERRA_MASTER_COMPENDIUM_REBUILT.pdf). The available original source is [APTERRA_MASTER_COMPENDIUM_EDITABLE.docx](../../APTERRA_MASTER_COMPENDIUM_EDITABLE.docx); the original PDF itself is not present in the workspace. Text extraction of both available sources confirms the same core capability-underwriting thesis and refund MVP, while the rebuilt PDF supplies the current product/build framing. No absence is silently treated as a superseding requirement.

Current preview deployment: Studio Dev / Studio Next alias, chain 61997, contract `0xfDB7f4C28F157662133dF51B5a864BDa3F4B7060`, public frontend https://apterra.vercel.app/. The former `0x4210...` deployment is historical only.

The original reviewed base `ac6ca287183eb651f59094da122d18ffb52e2852` and source/hash checkpoint `72f89d48e8f0f921a0b87d8c506bb9cc40bb8ea2` are historical traceability evidence, not the current main revision. Current preview identity is Studio Dev chain 61997, canonical contract `0xfDB7f4C28F157662133dF51B5a864BDa3F4B7060`, and public frontend https://apterra.vercel.app/. The application includes strict evidence/challenge schema validation, canonical local challenge-preimage recovery, per-session bounded challenge generation, wallet/network write guards, canonical on-chain version-history details, generated harness commands, exact registered `agent_ref` evidence binding, and same-hash automatic finality tracking. Historical source hashes remain useful for the checkpoint they identify; `PASS` means the cited check passed for the stated scope and does not convert direct-mode tests into live consensus proof.

## Core product and contract

| Requirement | Status | Implementation / contract state | Frontend flow | Tests / exact observed evidence | Live proof / gap |
|---|---|---|---|---|---|
| Capability underwriting, not score/identity registry/payment rail | PASS (boundary) | `contracts/apterra.py`; bounded refund capability only | Product copy names scoped capability authority | Source review; clean-clone production build passed at `58491a821d17ace5719cca3a4452801348fc165e` | No durable production claim or general safety score |
| Exact immutable version: agent/model/provider/adapter/policy/tools/runtime/harness | PARTIAL | `register_agent_version` binds provider ID plus version hashes; no update method | Registration fields separate provider and adapter; canonical version-history details are readable | Current pinned direct suite: 63/63; no TEE/provider attestation | Live v1 registration exists; execution provenance remains operator-asserted rather than externally attested |
| Version history and drift | PARTIAL | Immutable records, suspension, `get_agent_version`, append-only catalog | Reads all catalog IDs then fetches every bounded detail in batches; shows configuration hashes and fails closed to unknown status; selection reads canonical version | Parser unit tests; direct append-only/suspension tests | Live v1 registration/canonical records are documented in `LIVE_EVIDENCE.md`; material-change policy still depends on a new immutable ID and does not provide execution attestation |
| Versioned, immutable capability/policy/rubric/risk catalog | PASS (rebuilt MVP scope) | Immutable `refund-policy-v4.2` policy, rubric and risk profile are embedded and hash-bound in `contracts/apterra.py`; historical commitments cannot be mutated | Console exposes the fixed refund profile and exact commitments | Hash/mapping coverage in the direct suite; current suite 63/63 | Additional capability domains are explicitly V2 scope in the rebuilt compendium, not a Phase 1 failure |
| Claim binds exact version, scope, profile/amount, consumer, resource, approver and validity | PASS (current refund profile) | `create_claim` rejects bounds and consumer/approver role collision | Claim form asks for distinct consumer and approver | Direct role/bounds tests; current suite 63/63 | Live v1 claim readback is recorded in `LIVE_EVIDENCE.md` |
| Authorized challenge assignment precedes reveal | PASS (source/direct + live v1) | Owner-only `assign_challenge` stores commitment/executor/time; owner-only `reveal_challenge_inputs` verifies exact digest; submit rejects unrevealed | Commit → canonical verify → separate reveal controls | Current direct suite 63/63, including altered reveal, malformed identifiers and early submission | Live v1 assignment/reveal transactions are recorded in `LIVE_EVIDENCE.md` |
| Fresh/rotating challenge and contamination controls; no memorisable answer key | PARTIAL | Expected actions/amounts removed; browser cryptographic randomness varies case IDs and bounded details across the four required classes | Per-session generated challenge; exact preimage is hash-verified, restored after reload, and checked against canonical assignment before reveal | Frontend deterministic suite 43/43; Chromium smoke 3/3 | Bounded public phrase/fact catalog, not strong contamination resistance or general competence proof; generation is client/owner trusted, not server entropy or execution attestation |
| Reproducible disclosed harness for exact committed inputs | PARTIAL | `harness/run/run_harness.py` accepts canonical revealed cases; commits provider/model, environment, run ID, timings, tool trace hash; bounds URL, input, response and bundle | Download canonical revealed input; invoke disclosed local runner; upload generated bundle | Harness unit tests in the 63-test direct suite; live v1 provider evidence is recorded | Provider/output identity is asserted by executor; hash does not provide TEE/provider/process attestation |
| Evidence binds challenge/version/policy/rubric/executor/sender and exact per-case input/output | PASS (source/direct + live v1) | `submit_attempt` enforces exact schema-v3 envelope/case schemas, including `agent_ref` equality with the registered immutable version, committed case type/input, output hashes, raw action/amount, time, uniqueness, sender and bundle | File upload rejects >20,000 bytes rather than truncating; generated command checks the selected runner reference against the registered version reference | Current direct suite 63/63, including agent-reference mutation, extra-field, mismatch, malformed JSON and commitment rejection | Canonical live v1 evidence attempt is recorded in `LIVE_EVIDENCE.md` |
| Evidence execution provenance/trace honesty | PARTIAL | `run_id`, environment, timestamps and bounded tool-trace commitment; no tools/chain-of-thought recorded | Trust disclosure | Unit tests; actual provider execution NOT RUN | No TEE, provider, or process attestation; the harness is operator-trusted |
| Semantic GenLayer underwriting under fixed rubric; hostile input defense | PARTIAL | `underwrite_attempt` uses v0.6 `prompt_comparative`, bounded consequence fields and deterministic mapping | Prepare call and canonical judgment read | Current direct suite 63/63; GenVM lint/SDK validation, typecheck and 25-method schema pass | Live v1 semantic underwriting finalized DENY; no claim of universal/general capability judgment |
| Model errors, malformed/unsupported results, validator disagreement, retry and atomicity | PARTIAL | No persistent `UNDER_JUDGMENT`; transaction state remains pending externally; direct errors leave attempt retryable | Pending hash/finality tracker exists | Direct model-error retry and malformed-output cases; validator disagreement is mocked in direct mode | Consensus rollback, retry status and recovery need live tx proof |
| Exact CERTIFY/LIMIT/DENY/INCONCLUSIVE mapping | PASS (direct deterministic map + live DENY) | `_map_verdict`; amount and duration are contract-derived; DENY revokes, INCONCLUSIVE suspends, positive result replaces | Canonical judgment/effective warrant read | Direct verdict/replacement tests in current 63-test suite | Live v1 finalized DENY is recorded; positive verdict paths remain direct-tested rather than live-demonstrated |
| CERTIFY ceiling/expiry | PASS (source/direct) | Max `min(requested, $5,000)`, max 90 days | Effective authority read displays amount/expiry | Direct warrant tests | No live positive warrant; live v1 DENY correctly leaves $0 effective authority |
| LIMIT $100 / 14 days and above-limit human approval | PARTIAL | On-chain `approve_limit_override` binds approver, warrant, operation, amount, consumer/resource, expiry, one-use nonce; adapter consumes exact approval | Separate approver flow exists; needs usability/readback polish | Direct approval binding/role/replay tests; 58 passed on clean clone | No live approval or browser proof; end-to-end above-$100 path unverified |
| DENY retained receipt and revocation; INCONCLUSIVE suspension | PASS (direct + live DENY) | Receipts/warrants preserved; effective pointer removed/suspended | Judgment and warrant reads | Direct replacement/history tests | Live v1 DENY finalized on Studio Dev and effective authority is $0 |
| Warrant history, expiry and suspended-version correctness | PARTIAL | `get_warrant_history`, effective authority enforces version and expiry; history append-only | Read buttons added | Direct expiry/suspension/history tests; 58 passed on clean clone | Browser history UI not polished/live-verified |
| Downstream permission gate performs protected action and blocks violations | PARTIAL | `execute_sandbox_refund` rechecks authority and records a sandbox adapter action atomically; no funds transfer | Adapter action and outcome read controls | Direct allowed/overlimit/approval/replay tests | Live v1 protected $600 action was blocked after DENY; no adapter action/receipt persisted and no real funds moved |
| Atomicity/races/nonce/consumer/resource/action/expiry | PARTIAL | One-use nonce and effective warrant pointer; GenLayer execution should revert atomically | Error display is generic for many contract reverts | Current pinned direct suite: 63/63, including replay/expiry guards | Concurrent transaction/finality behavior is not live stress-tested |
| Public contract reads for versions/claims/challenges/attempts/findings/receipts/warrant history/effective authority/adapter | PARTIAL | Current getters cover individual records; version IDs and warrant history are indexed | Read buttons for selected records | Linter schema lists 25 methods (11 writes, 14 views) | Claim/attempt pagination and readable field-specific error mapping absent |

## User flows, engineering and evidence

| Requirement | Status | Implementation / test | Live proof / gap |
|---|---|---|---|
| Wallet account/network clarity; account and chain changes; wrong-network fail-closed | PARTIAL | `src/lib/network.ts`; deterministic frontend suite 43/43 includes refresh/account/chain/manual-disconnect plus fresh-network direct/wrapped-4902 onboarding | Injected EIP-1193 only; mobile signing requires an injected-wallet/dapp browser | Public app is live, but final CI does not emulate a real wallet extension/signature session |
| Fresh, method-specific fee quote and explicit transaction review | PARTIAL | `src/lib/transactions.ts`; fee-policy fingerprint gate and review modal | Fresh quote is required for every browser write | Live v1 transactions exist, but the final browser review path was not independently re-executed with a real extension in CI |
| Distinct signature/submission/consensus/finality/execution/readback; no blind retries | PARTIAL | Submitted hashes persist; newly submitted writes automatically track the same hash to finality; decision/finality buttons remain fallback; success requires accepted + finalized + `FINISHED_WITH_RETURN`, and mapped writes require canonical ID readback | Receipt & finality preserves the same transaction hash; no automatic resubmission path is added | Live browser recovery under extension disconnect/network interruption remains untested |
| Guided claim → assign → reveal → run → submit → underwrite → inspect → adapter flow | PARTIAL | Routed UI provides every Phase 1 stage; challenge download/harness invocation and canonical reads are present | Canonical live v1 lifecycle is recorded, including DENY and blocked $600 action | Full browser-driven end-to-end with real injected wallet remains unverified |
| Responsive, accessible, keyboard/screen-reader UI and field errors | NOT RUN | Existing responsive CSS; no systematic accessibility audit or full error-state tests | Audit/browser keyboard and assistive testing required |
| CI from clean checkout; frontend lint/typecheck/tests/build, Python/direct tests, GenVM check/schema/typecheck | PASS (workflow) | Current CI passes Studio Dev guard + ESLint + TypeScript, frontend tests 43/43, Chromium smoke 3/3, production build, GenVM lint (3/3 + SDK validation), SDK typecheck, schema (25 methods: 14 views/11 writes), and direct suite 63/63 | Direct-mode/CI evidence is not live consensus proof; real injected-wallet extension flow remains outside CI |
| Frontend/browser E2E for full product, roles, recovery, authoritative reads and adapter | PARTIAL (coverage breadth) | Playwright Chromium smoke: 3/3 (unconfigured fail-closed + injected-wallet guidance, oversized evidence rejection, verified challenge-preimage reload recovery) | No full wallet extension flow, canonical on-chain transaction flow, or adapter browser execution in automated E2E |
| Studio Dev 61997 deployment/finalized lifecycle | PASS | Canonical deployment `0xfDB7f4C28F157662133dF51B5a864BDa3F4B7060`; tx and source readback recorded in `docs/LIVE_EVIDENCE.md` | v1 live lifecycle finalized |
| Public frontend production deployment | DEPLOYED (manual) | Public app: https://apterra.vercel.app/; configured for canonical Studio Dev contract | Public URL available; v1 live evidence is recorded |
| Clean clone, reproducible transcript, final exact commit | PASS (current CI baseline) | Locked dependencies; Studio Dev guard/lint/typecheck, frontend 43/43, browser smoke 3/3, production build, GenVM lint/typecheck/schema, and direct suite 63/63 all pass in hosted CI | Historical Windows clean-clone transcript remains reproducibility evidence; current hosted CI is the release gate |
| Submission/demo package / pitch / live links | PARTIAL | Canonical contract/explorer and public frontend links are present in README and `LIVE_EVIDENCE.md`; live v1 evidence is documented | No recording/pitch artifact is asserted here |

## Phase boundary / explicit non-goals

No token, identity registry, leaderboard, custodial escrow, external bridge, real refund processor, marketplace, or Phase 2 integration is implemented or implied. Studio Dev addresses and transactions, once obtained, are ephemeral preview evidence. No production-agent identity or TEE attestation is claimed.

## Source reconciliation

The compendiums define capability-specific, version-bound underwriting, evidence, deterministic warrants, and downstream enforceability; they do not treat a general score or generic badge as the product. The refund-policy flow remains one vertical slice. The rows above explicitly mark the missing breadth, browser/live proof, and external approvals instead of calling the slice complete.
