# APTERRA Compendium Requirements & Evidence Ledger

This is the single authoritative Phase 1 traceability/evidence ledger. The latest rebuilt compendium is [APTERRA_MASTER_COMPENDIUM_REBUILT.pdf](C:/Users/DELL/Downloads/APTERRA_MASTER_COMPENDIUM_REBUILT.pdf) (20 pages). The original compendium PDF was not present in the supplied files or attachment cache; the available original source is [APTERRA_MASTER_COMPENDIUM_EDITABLE.docx](C:/Users/DELL/Downloads/APTERRA_MASTER_COMPENDIUM_EDITABLE.docx). The editable source was reconciled with the rebuilt PDF. No absence is silently treated as a superseding requirement.

The reviewed base is `ac6ca287183eb651f59094da122d18ffb52e2852`. Current pushed code candidate is `cc6a35e339d0db1e8b2c75149747b51c84f7b217`; it includes strict contract evidence/challenge schema validation, hash-verified local challenge preimage recovery, canonical assignment matching before reveal, active wallet identity/network rechecks before signing, bounded per-session randomized challenge inputs, Next.js/PostCSS security updates, and consistent LF checkout. Contract source SHA-256 is `34b995a9b0b5b9b75bb2f29bb5793eae213171a1c26026e4cbd8d4ca903121eb`; web entry SHA-256 is `aa421907bccaaebcfa02e3f152714d2ec5d09f39deb00b05bf6fe4b45219b738`. The clean-clone verification at `8e753553b51b1e7de755390ecf61534ba49341b8` covers identical application sources/dependencies; `cc6a35e` adds only `.gitattributes`, and its new clone confirms identical raw source hashes. `PASS` means the cited local automated/source check passed for that specific requirement; it is not a live Studio Dev or production deployment pass. `BLOCKED` means a required external approval/credential is unavailable. `NOT RUN` means no evidence was produced. No row implies overall product completion.

## Core product and contract

| Requirement | Status | Implementation / contract state | Frontend flow | Tests / exact observed evidence | Live proof / gap |
|---|---|---|---|---|---|
| Capability underwriting, not score/identity registry/payment rail | PASS (boundary) | `contracts/apterra.py`; bounded refund capability only | Product copy names scoped capability authority | Source review; clean-clone production build passed at `58491a821d17ace5719cca3a4452801348fc165e` | No durable production claim or general safety score |
| Exact immutable version: agent/model/provider/adapter/policy/tools/runtime/harness | PARTIAL | `register_agent_version` binds provider ID plus version hashes; no update method | Registration fields separate provider and adapter | Pinned direct suite: 57 passed on clean clone; no real provider run/attestation | Version list UX incomplete; no execution attestation |
| Version history and drift | PARTIAL | Immutable records, suspension, `get_agent_version`, append-only version catalog | Selected-version read only | Pinned direct suite includes append-only/version suspension cases | No browser history list |
| Versioned, immutable capability/policy/rubric/risk catalog | FAIL (breadth) | Frozen `refund-policy-v4.2` only; updated risk hash `8e3e4d4f442675771bab7b8bd5cb31f77e83683eed025be25bcce4593a3171c7` | One refund profile | Hash and mapping tests | No version catalog for other compendium capabilities/profiles |
| Claim binds exact version, scope, profile/amount, consumer, resource, approver and validity | PASS (current refund profile) | `create_claim` rejects bounds and consumer/approver role collision | Claim form asks for distinct consumer and approver | Direct role/bounds tests; 57 passed on clean clone | No live claim readback |
| Authorized challenge assignment precedes reveal | PASS (source/direct) | Owner-only `assign_challenge` stores commitment/executor/time; owner-only `reveal_challenge_inputs` verifies exact digest; submit rejects unrevealed | Commit → canonical verify → separate reveal controls | Pinned direct regression suite: 57 passed, including altered reveal, malformed identifiers and early submission | No live challenge transactions |
| Fresh/rotating challenge and contamination controls; no memorisable answer key | PARTIAL | Expected actions/amounts removed; browser cryptographic randomness now varies case IDs, bounded amounts, timing, evidence details, ambiguity details and adversarial phrasing across four required classes | Per-session generated challenge; exact preimage is hash-verified, restored after reload, and compared to canonical assignment bindings before reveal | Frontend unit suite 11/11; browser suite reported all 3 scenarios passing, although the Playwright process did not exit cleanly in this Windows run; direct case schema checks | This remains a bounded public phrase/fact catalog, not strong contamination resistance or general competence proof; generation is client/owner trusted, not server entropy or execution attestation |
| Reproducible disclosed harness for exact committed inputs | PARTIAL | `harness/run/run_harness.py` accepts canonical revealed cases; commits provider/model, environment, run ID, timings, tool trace hash; bounds URL, input, response and bundle | Download canonical revealed input; invoke disclosed local runner; upload generated bundle | Harness unit tests in direct suite; provider has not been run in this task | Provider/output identity is asserted by executor; hash does not attest production execution |
| Evidence binds challenge/version/policy/rubric/executor/sender and exact per-case input/output | PASS (source/direct) | `submit_attempt` enforces exact v2 envelope/case schemas, committed case type/input, output hashes, raw action/amount, time, uniqueness, sender and bundle | File upload rejects >20,000 bytes rather than truncating | Pinned direct suite: 57 passed, including extra-field, mismatch, malformed JSON and mutation rejection | No canonical live evidence attempt |
| Evidence execution provenance/trace honesty | PARTIAL | `run_id`, environment, timestamps and bounded tool-trace commitment; no tools/chain-of-thought recorded | Trust disclosure | Unit tests; actual provider execution NOT RUN | No TEE, provider, or process attestation; the harness is operator-trusted |
| Semantic GenLayer underwriting under fixed rubric; hostile input defense | PARTIAL | `underwrite_attempt` uses v0.6 `prompt_comparative`, bounded consequence fields and deterministic mapping | Prepare call and canonical judgment read | Direct suite: 57 passed; pinned GenVM check (3 lint checks, SDK validation), typecheck and schema passed | No real semantic Studio Dev transaction |
| Model errors, malformed/unsupported results, validator disagreement, retry and atomicity | PARTIAL | No persistent `UNDER_JUDGMENT`; transaction state remains pending externally; direct errors leave attempt retryable | Pending hash/finality tracker exists | Direct model-error retry and malformed-output cases; validator disagreement is mocked in direct mode | Consensus rollback, retry status and recovery need live tx proof |
| Exact CERTIFY/LIMIT/DENY/INCONCLUSIVE mapping | PASS (direct deterministic map) | `_map_verdict`; amount and duration are contract-derived; DENY revokes, INCONCLUSIVE suspends, positive result replaces | Canonical judgment/effective warrant read | Direct verdict/replacement tests; 57 passed on clean clone | No finalized real verdicts |
| CERTIFY ceiling/expiry | PASS (source/direct) | Max `min(requested, $5,000)`, max 90 days | Effective authority read displays amount/expiry | Direct warrant tests | No live warrant |
| LIMIT $100 / 14 days and above-limit human approval | PARTIAL | On-chain `approve_limit_override` binds approver, warrant, operation, amount, consumer/resource, expiry, one-use nonce; adapter consumes exact approval | Separate approver flow exists; needs usability/readback polish | Direct approval binding/role/replay tests; 57 passed on clean clone | No live approval or browser proof; end-to-end above-$100 path unverified |
| DENY retained receipt and revocation; INCONCLUSIVE suspension | PASS (direct) | Receipts/warrants preserved; effective pointer removed/suspended | Judgment and warrant reads | Direct replacement/history tests | No finalized Studio Dev outcomes |
| Warrant history, expiry and suspended-version correctness | PARTIAL | `get_warrant_history`, effective authority enforces version and expiry; history append-only | Read buttons added | Direct expiry/suspension/history tests; 57 passed on clean clone | Browser history UI not polished/live-verified |
| Downstream permission gate performs protected action and blocks violations | PARTIAL | `execute_sandbox_refund` rechecks authority and records a sandbox adapter action atomically; no funds transfer | Adapter action and outcome read controls | Direct allowed/overlimit/approval/replay tests added | No live action proof; clearly not a real refund/transfer |
| Atomicity/races/nonce/consumer/resource/action/expiry | PARTIAL | One-use nonce and effective warrant pointer; GenLayer execution should revert atomically | Error display is generic for many contract reverts | Focused direct tests; final rerun pending | Concurrent transaction/finality behavior not live-tested |
| Public contract reads for versions/claims/challenges/attempts/findings/receipts/warrant history/effective authority/adapter | PARTIAL | Current getters cover individual records; version IDs and warrant history are indexed | Read buttons for selected records | Linter schema lists 25 methods (11 writes, 14 views) | Claim/attempt pagination and readable field-specific error mapping absent |

## User flows, engineering and evidence

| Requirement | Status | Implementation / test | Live proof / gap |
|---|---|---|---|
| Wallet account/network clarity; account and chain changes; wrong-network fail-closed | PARTIAL | `src/lib/network.ts`; frontend unit suite: 11/11, including active account/network recheck; real-browser fail-closed smoke passed | No injected-wallet/signature session was run |
| Fresh, method-specific fee quote and explicit transaction review | PARTIAL | `src/lib/transactions.ts`; fee-policy fingerprint gate and review modal | Fee profile needs fresh matching quote for every live transaction; no approved write |
| Distinct signature/submission/consensus/finality/execution/readback; no blind retries | PARTIAL | Pending transaction hashes persist; decision and finality are separate; finality requires accepted + finalized + `FINISHED_WITH_RETURN`; mapped writes perform a canonical record readback and label mismatch/missing/error | Explorer URLs and transaction tracking are visible; live wallet recovery not tested |
| Guided claim → assign → reveal → run → submit → underwrite → inspect → adapter flow | PARTIAL | Main page provides steps; challenge download/harness invocation added | Full live flow blocked pending approved deployment/wallet signatures and must be browser-tested |
| Responsive, accessible, keyboard/screen-reader UI and field errors | NOT RUN | Existing responsive CSS; no systematic accessibility audit or full error-state tests | Audit/browser keyboard and assistive testing required |
| CI from clean checkout; frontend lint/typecheck/tests/build, Python/direct tests, GenVM check/schema/typecheck | PARTIAL | Clean clone `8e753553b51b1e7de755390ecf61534ba49341b8` passed npm ci, lint/typecheck, 11 frontend tests, production build, 57 direct tests, GenVM check/SDK validation, schema and typecheck; `cc6a35e` changes only line-ending policy | Remote GitHub workflow not run; five moderate development-only npm advisories remain, while production audit is clean |
| Frontend/browser E2E for full product, roles, recovery, authoritative reads and adapter | FAIL (coverage breadth) | Playwright Chromium smoke: 3/3 on clean clone `8e753553b51b1e7de755390ecf61534ba49341b8` (unconfigured fail-closed, oversized evidence rejection, verified challenge-preimage reload recovery); synthetic local draft is test-only | No full wallet flow, role mutation, canonical on-chain reads, transaction recovery or adapter browser proof |
| Studio Dev 61997 deployment/finalized lifecycle | NOT RUN (release candidate incomplete) | Current read-only CLI verification confirms RC `studio-dev` maps to 61997/canonical RPC and direct `eth_chainId=0xf22d`; existing designated account is active/unlocked. Prior 200 GEN balance is historical and not a current preflight | No contract address, fresh balance/fee quote, or transaction proposal: complete product and full E2E acceptance are not ready. Owner signature remains mandatory after a candidate passes preflight |
| Public frontend production deployment | BLOCKED (Vercel auth/project) | No `.vercel` project link; connector reports no teams; CLI read failed with `EACCES` and network access is unavailable | Owner must connect/authorize the intended Vercel account and project, or supply access through the approved integration |
| Clean clone, reproducible transcript, final exact commit | PARTIAL | Clean clone verification at `8e753553b51b1e7de755390ecf61534ba49341b8`; source-only line-ending policy candidate `cc6a35e` cloned cleanly and matching raw source hashes verified | Final docs evidence refresh commit and remote GitHub Actions result not available |
| Submission/demo package / pitch / live links | NOT RUN | README/docs need reconciliation | No live links, final demos or recording; preview explorer evidence unavailable |

## Phase boundary / explicit non-goals

No token, identity registry, leaderboard, custodial escrow, external bridge, real refund processor, marketplace, or Phase 2 integration is implemented or implied. Studio Dev addresses and transactions, once obtained, are ephemeral preview evidence. No production-agent identity or TEE attestation is claimed.

## Source reconciliation

The compendiums define capability-specific, version-bound underwriting, evidence, deterministic warrants, and downstream enforceability; they do not treat a general score or generic badge as the product. The refund-policy flow remains one vertical slice. The rows above explicitly mark the missing breadth, browser/live proof, and external approvals instead of calling the slice complete.
