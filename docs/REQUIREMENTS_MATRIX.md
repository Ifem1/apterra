# APTERRA Compendium Requirements & Evidence Ledger

This is the single authoritative Phase 1 traceability/evidence ledger. The latest rebuilt compendium is the [20-page APTERRA_MASTER_COMPENDIUM_REBUILT.pdf](../../APTERRA_MASTER_COMPENDIUM_REBUILT.pdf). The available original source is [APTERRA_MASTER_COMPENDIUM_EDITABLE.docx](../../APTERRA_MASTER_COMPENDIUM_EDITABLE.docx); the original PDF itself is not present in the workspace. Text extraction of both available sources confirms the same core capability-underwriting thesis and refund MVP, while the rebuilt PDF supplies the current product/build framing. No absence is silently treated as a superseding requirement.

Current preview deployment: Studio Dev / Studio Next alias, chain 61997, contract `0x4210D8556c7e447bA70D12321ffBA1cE19eB77dd`, public frontend https://apterra.vercel.app/.

The reviewed base is `ac6ca287183eb651f59094da122d18ffb52e2852`. Current application source commit is `72f89d48e8f0f921a0b87d8c506bb9cc40bb8ea2`; this ledger refresh follows as a documentation-only commit. The application includes strict evidence/challenge schema validation, canonical local challenge-preimage recovery, per-session bounded challenge generation, wallet/network write guards, canonical on-chain version-history details, a generated harness command, and exact registered `agent_ref` evidence binding under schema v3. Contract SHA-256 is `b6301cb4cd9789819dfe911af8ea0fc4907eaa0272294c2260a7aa998699841d`; web entry SHA-256 is `35a6d7b40522d0a04c795629cfb63d5dddb4abd0c6407b2826faf97fb5876ba0`; version-history parser SHA-256 is `aa93c34dbb10485b397b20b7b8e1e13d0a794052ec704de173a40721cbcb7435`; harness command helper SHA-256 is `162538844fbefec823d5d71904a837e750b90539acf4ebb14c5b7bb91eb7cf14`; harness runner SHA-256 is `82bc882fb5ceb5d7cf98ed2bc2dcae8918ef9a0e494f486354d7d9982650c11f`. `PASS` means the cited local automated/source check passed for that specific requirement; it is not a live Studio Dev or production deployment pass. `BLOCKED` means a required external approval/credential is unavailable. `NOT RUN` means no evidence was produced. No row implies overall product completion.

## Core product and contract

| Requirement | Status | Implementation / contract state | Frontend flow | Tests / exact observed evidence | Live proof / gap |
|---|---|---|---|---|---|
| Capability underwriting, not score/identity registry/payment rail | PASS (boundary) | `contracts/apterra.py`; bounded refund capability only | Product copy names scoped capability authority | Source review; clean-clone production build passed at `58491a821d17ace5719cca3a4452801348fc165e` | No durable production claim or general safety score |
| Exact immutable version: agent/model/provider/adapter/policy/tools/runtime/harness | PARTIAL | `register_agent_version` binds provider ID plus version hashes; no update method | Registration fields separate provider and adapter; canonical version-history details are readable | Pinned direct suite: 58 passed on clean clone; no real provider run/attestation | No execution attestation |
| Version history and drift | PARTIAL | Immutable records, suspension, `get_agent_version`, append-only catalog | Reads all catalog IDs then fetches every bounded detail in batches; shows configuration hashes and fails closed to unknown status; selection reads canonical version | Parser unit tests; direct append-only/suspension tests | No deployed canonical read or live version registration; material-change policy is enforced by requiring a new immutable ID, not by external attestation |
| Versioned, immutable capability/policy/rubric/risk catalog | PASS (rebuilt MVP scope) | Immutable `refund-policy-v4.2` policy, rubric and risk profile are embedded and hash-bound in `contracts/apterra.py`; historical commitments cannot be mutated | Console exposes the fixed refund profile and exact commitments | Hash/mapping coverage in the direct suite; 58 tests pass on the clean clone | Additional capability domains are explicitly V2 scope in the rebuilt compendium, not a Phase 1 failure |
| Claim binds exact version, scope, profile/amount, consumer, resource, approver and validity | PASS (current refund profile) | `create_claim` rejects bounds and consumer/approver role collision | Claim form asks for distinct consumer and approver | Direct role/bounds tests; 58 passed on clean clone | No live claim readback |
| Authorized challenge assignment precedes reveal | PASS (source/direct) | Owner-only `assign_challenge` stores commitment/executor/time; owner-only `reveal_challenge_inputs` verifies exact digest; submit rejects unrevealed | Commit → canonical verify → separate reveal controls | Pinned direct regression suite: 58 passed, including altered reveal, malformed identifiers and early submission | No live challenge transactions |
| Fresh/rotating challenge and contamination controls; no memorisable answer key | PARTIAL | Expected actions/amounts removed; browser cryptographic randomness varies case IDs and bounded details across the four required classes | Per-session generated challenge; exact preimage is hash-verified, restored after reload, and checked against canonical assignment before reveal | Frontend unit suite 15/15; three Playwright smoke tests pass in GitHub CI (local Windows Playwright process still does not exit cleanly) | Bounded public phrase/fact catalog, not strong contamination resistance or general competence proof; generation is client/owner trusted, not server entropy or execution attestation |
| Reproducible disclosed harness for exact committed inputs | PARTIAL | `harness/run/run_harness.py` accepts canonical revealed cases; commits provider/model, environment, run ID, timings, tool trace hash; bounds URL, input, response and bundle | Download canonical revealed input; invoke disclosed local runner; upload generated bundle | Harness unit tests in direct suite; provider has not been run in this task | Provider/output identity is asserted by executor; hash does not attest production execution |
| Evidence binds challenge/version/policy/rubric/executor/sender and exact per-case input/output | PASS (source/direct) | `submit_attempt` enforces exact schema-v3 envelope/case schemas, including `agent_ref` equality with the registered immutable version, committed case type/input, output hashes, raw action/amount, time, uniqueness, sender and bundle | File upload rejects >20,000 bytes rather than truncating; generated command checks the selected runner reference against the registered version reference | Pinned direct suite: 58 passed, including agent-reference mutation, extra-field, mismatch, malformed JSON and commitment rejection | No canonical live evidence attempt |
| Evidence execution provenance/trace honesty | PARTIAL | `run_id`, environment, timestamps and bounded tool-trace commitment; no tools/chain-of-thought recorded | Trust disclosure | Unit tests; actual provider execution NOT RUN | No TEE, provider, or process attestation; the harness is operator-trusted |
| Semantic GenLayer underwriting under fixed rubric; hostile input defense | PARTIAL | `underwrite_attempt` uses v0.6 `prompt_comparative`, bounded consequence fields and deterministic mapping | Prepare call and canonical judgment read | Direct suite: 58 passed; pinned GenVM check (3 lint checks, SDK validation), typecheck and schema passed | No real semantic Studio Dev transaction |
| Model errors, malformed/unsupported results, validator disagreement, retry and atomicity | PARTIAL | No persistent `UNDER_JUDGMENT`; transaction state remains pending externally; direct errors leave attempt retryable | Pending hash/finality tracker exists | Direct model-error retry and malformed-output cases; validator disagreement is mocked in direct mode | Consensus rollback, retry status and recovery need live tx proof |
| Exact CERTIFY/LIMIT/DENY/INCONCLUSIVE mapping | PASS (direct deterministic map) | `_map_verdict`; amount and duration are contract-derived; DENY revokes, INCONCLUSIVE suspends, positive result replaces | Canonical judgment/effective warrant read | Direct verdict/replacement tests; 58 passed on clean clone | No finalized real verdicts |
| CERTIFY ceiling/expiry | PASS (source/direct) | Max `min(requested, $5,000)`, max 90 days | Effective authority read displays amount/expiry | Direct warrant tests | No live warrant |
| LIMIT $100 / 14 days and above-limit human approval | PARTIAL | On-chain `approve_limit_override` binds approver, warrant, operation, amount, consumer/resource, expiry, one-use nonce; adapter consumes exact approval | Separate approver flow exists; needs usability/readback polish | Direct approval binding/role/replay tests; 58 passed on clean clone | No live approval or browser proof; end-to-end above-$100 path unverified |
| DENY retained receipt and revocation; INCONCLUSIVE suspension | PASS (direct) | Receipts/warrants preserved; effective pointer removed/suspended | Judgment and warrant reads | Direct replacement/history tests | No finalized Studio Dev outcomes |
| Warrant history, expiry and suspended-version correctness | PARTIAL | `get_warrant_history`, effective authority enforces version and expiry; history append-only | Read buttons added | Direct expiry/suspension/history tests; 58 passed on clean clone | Browser history UI not polished/live-verified |
| Downstream permission gate performs protected action and blocks violations | PARTIAL | `execute_sandbox_refund` rechecks authority and records a sandbox adapter action atomically; no funds transfer | Adapter action and outcome read controls | Direct allowed/overlimit/approval/replay tests added | No live action proof; clearly not a real refund/transfer |
| Atomicity/races/nonce/consumer/resource/action/expiry | PARTIAL | One-use nonce and effective warrant pointer; GenLayer execution should revert atomically | Error display is generic for many contract reverts | Latest pinned direct suite: 58 passed, including replay/expiry guards | Concurrent transaction/finality behavior not live-tested |
| Public contract reads for versions/claims/challenges/attempts/findings/receipts/warrant history/effective authority/adapter | PARTIAL | Current getters cover individual records; version IDs and warrant history are indexed | Read buttons for selected records | Linter schema lists 25 methods (11 writes, 14 views) | Claim/attempt pagination and readable field-specific error mapping absent |

## User flows, engineering and evidence

| Requirement | Status | Implementation / test | Live proof / gap |
|---|---|---|---|
| Wallet account/network clarity; account and chain changes; wrong-network fail-closed | PARTIAL | `src/lib/network.ts`; frontend unit suite: 15/15, including active account/network recheck; real-browser fail-closed smoke passed | No injected-wallet/signature session was run |
| Fresh, method-specific fee quote and explicit transaction review | PARTIAL | `src/lib/transactions.ts`; fee-policy fingerprint gate and review modal | Fee profile needs fresh matching quote for every live transaction; no approved write |
| Distinct signature/submission/consensus/finality/execution/readback; no blind retries | PARTIAL | Pending transaction hashes persist; decision and finality are separate; finality requires accepted + finalized + `FINISHED_WITH_RETURN`; mapped writes perform a canonical record readback and label mismatch/missing/error | Explorer URLs and transaction tracking are visible; live wallet recovery not tested |
| Guided claim → assign → reveal → run → submit → underwrite → inspect → adapter flow | PARTIAL | Main page provides steps; challenge download/harness invocation added | Full live flow blocked pending approved deployment/wallet signatures and must be browser-tested |
| Responsive, accessible, keyboard/screen-reader UI and field errors | NOT RUN | Existing responsive CSS; no systematic accessibility audit or full error-state tests | Audit/browser keyboard and assistive testing required |
| CI from clean checkout; frontend lint/typecheck/tests/build, Python/direct tests, GenVM check/schema/typecheck | PASS (workflow); PASS (manual clone except clean Playwright exit) | Latest GitHub run [34855094116](https://github.com/Ifem1/apterra/actions/runs/34855094116) passed on `72f89d4`: 15 frontend tests, browser smoke 3/3, production build, GenVM check (3 lint checks + SDK validation, 25 methods), SDK typecheck, schema (25 methods, 14 views/11 writes), and direct suite (58). Fresh Windows clone `apterra-verify-final-53973ce-20260914`, fast-forwarded to exact `72f89d4`, passed `npm ci` (503 locked packages), lint, frontend tests 15/15, production build, and direct suite 58/58 in 27.05s. GenVM check/typecheck/schema passed on that exact source prior to the repeat npm install. | Local manual E2E process did not exit cleanly; CI passed all 3 smoke tests. Full audit at prior lockfile state found 0 production advisories and 5 moderate development-only advisories |
| Frontend/browser E2E for full product, roles, recovery, authoritative reads and adapter | FAIL (coverage breadth) | Playwright Chromium smoke: 3/3 on clean clone `8e753553b51b1e7de755390ecf61534ba49341b8` (unconfigured fail-closed, oversized evidence rejection, verified challenge-preimage reload recovery); synthetic local draft is test-only | No full wallet flow, role mutation, canonical on-chain reads, transaction recovery or adapter browser proof |
| Studio Dev 61997 deployment/finalized lifecycle | PARTIAL | Finalized deployment exists at `0x4210D8556c7e447bA70D12321ffBA1cE19eB77dd`; deployment tx and owner readback are recorded in `docs/LIVE_EVIDENCE.md` | Live underwriting submission remains blocked by Studio RPC availability and commitment-diagnosis completion |
| Public frontend production deployment | DEPLOYED (manual) | Public app: https://apterra.vercel.app/; configured for Studio Dev contract `0x4210D8556c7e447bA70D12321ffBA1cE19eB77dd` | Public browser live-wallet flow and canonical lifecycle remain to be reverified |
| Clean clone, reproducible transcript, final exact commit | PASS for code at `72f89d4` | Manual clean clone `apterra-verify-final-53973ce-20260914` fast-forwarded to exact code `72f89d48e8f0f921a0b87d8c506bb9cc40bb8ea2`; `npm ci`, lint, frontend 15/15, production build, and direct suite 58/58 in 27.05s passed; GenVM check/typecheck/schema passed at that source. Clone stayed clean. | Local manual Playwright process did not exit cleanly; GitHub run `34855094116` passed all 3 browser smoke tests. This is not live consensus proof |
| Submission/demo package / pitch / live links | NOT RUN | README/docs need reconciliation | No live links, final demos or recording; preview explorer evidence unavailable |

## Phase boundary / explicit non-goals

No token, identity registry, leaderboard, custodial escrow, external bridge, real refund processor, marketplace, or Phase 2 integration is implemented or implied. Studio Dev addresses and transactions, once obtained, are ephemeral preview evidence. No production-agent identity or TEE attestation is claimed.

## Source reconciliation

The compendiums define capability-specific, version-bound underwriting, evidence, deterministic warrants, and downstream enforceability; they do not treat a general score or generic badge as the product. The refund-policy flow remains one vertical slice. The rows above explicitly mark the missing breadth, browser/live proof, and external approvals instead of calling the slice complete.
