"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
export default function SubpageShell({children}:{children:React.ReactNode}){const [theme,setTheme]=useState("system"),[account,setAccount]=useState<string|null>(null),[path,setPath]=useState("");useEffect(()=>{const v=localStorage.getItem("apterra:theme")||"system";setTheme(v);setPath(window.location.pathname);document.documentElement.dataset.theme=v;},[]);const change=(v:string)=>{setTheme(v);localStorage.setItem("apterra:theme",v);document.documentElement.dataset.theme=v};const connect=async()=>{const p=(window as Window & {ethereum?:{request:(a:{method:string})=>Promise<unknown>}}).ethereum;if(!p)return;const a=await p.request({method:"eth_requestAccounts"});if(Array.isArray(a)&&typeof a[0]==="string")setAccount(a[0])};const nav=(href:string,label:string)=><Link href={href} aria-current={path===href?"page":undefined} className={path===href?"active-nav":""}>{label}</Link>;return <><header className="topbar"><Link className="brand" href="/"><span className="brand-mark">A</span><span>APTERRA</span></Link><nav aria-label="Primary navigation">{nav("/underwriting","Underwriting")}{nav("/evidence","Evidence")}{nav("/authority","Authority")}</nav><div className="top-actions"><span className="network-pill"><i/> STUDIO DEV · 61997</span><label className="theme-control">THEME<select aria-label="Theme preference" value={theme} onChange={e=>change(e.target.value)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label><button className="wallet-button" onClick={()=>void connect()}>{account?`${account.slice(0,6)}…${account.slice(-4)}`:"Connect wallet"}</button></div></header>{children}</>}
